import React from 'react';
import Pagination from './Pagination';

const Filter = props => {
    return (
        <div className="filter">
            <Pagination />
        </div>
    );
};

export default Filter;
